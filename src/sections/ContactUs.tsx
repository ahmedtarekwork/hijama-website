import SectionWrapper from "../components/SectionWrapper";
import ContactList from "../components/ContactList";

const ContactUs = () => {
  return (
    <SectionWrapper id="contact-us" title="تواصل معنا" className="pb-5">
      <ContactList />
    </SectionWrapper>
  );
};
export default ContactUs;
